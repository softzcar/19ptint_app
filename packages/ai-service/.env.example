MAX_LADO_PX=4000
REMBG_MODEL=u2netp
UPSCALE_SCALE=2
# DTF UV: generar_patron_ia() -- sin esto, ese endpoint responde 503 y el
# resto del servicio funciona igual.
GEMINI_API_KEY=
# REALESRGAN_BIN se autodetecta según el SO (bin/macos o bin/ubuntu); solo
# definirlo acá si se quiere apuntar a otra ruta.
